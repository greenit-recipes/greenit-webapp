query recipe($id: String!){
  recipe(id: $id){
    id
    name
    image
    ingredients {
      id
      name
    }
    utensils {
      id
      name
    }
    description
    tags {
      id
      name
    }
    instructions
  }
}
