query recipe($id: String!) {
  recipe(id: $id) {
    id
    name
    image
    ingredients {
      id
      name
      amount
    }
    utensils {
      id
      name
    }
    notesFromAuthor
    expiry
    videoUrl
    description
    tags {
      id
      name
    }
    instructions
  }
}
